import { Typography } from '@mui/material';

function NodianLogo() {
  return (
    <Typography
      sx={{
        color: '#d2d6dc',
        stroke: 'currentcolor',
        opacity: 0.25,
        fontFamily: 'monospace',
      }}
      variant="h3"
    >
      NODIAN
    </Typography>
  );
}

export default NodianLogo;
